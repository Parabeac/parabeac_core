abstract class Rect {
  Rect(
    this.x,
    this.y,
    this.width,
    this.height,
  );

  double x;
  double y;
  double width;
  double height;

  // Rect.fromJson(Map<String, dynamic> json);
  // Map<String, dynamic> toJson();
}
